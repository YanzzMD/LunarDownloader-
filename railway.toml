[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn server:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120 --worker-class gthread"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "lunarytdl"
